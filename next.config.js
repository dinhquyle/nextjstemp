/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : ["placeimg.com","media.geeksforgeeks.org","localhost"],
  }
}

module.exports = nextConfig
